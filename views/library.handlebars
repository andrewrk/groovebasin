{{#if artists}}
  <ul id="library-items">
    {{#artists}}
      <li>
        <div class="artist hoverable expandable">
          <div class="ui-icon ui-icon-triangle-1-e"></div>
          <span>{{name}}</span>
        </div>
        <ul></ul>
      </li>
    {{/artists}}
  </ul>
{{else}}
  {{empty_library_message}}
{{/if}}

